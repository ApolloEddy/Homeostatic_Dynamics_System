**[返回目录](../INDEX.md)** | **[返回本章](./README.md)** | **[上一节](./07.md)** | **下一节：—**

# 05.8 巩固调度器（Consolidation Scheduler）
### 8.1 设计原理

巩固调度器负责定期触发离线巩固，避免手动触发。

### 8.2 调度策略

#### 8.2.1 时间间隔调度

时间间隔调度：

$$
\text{if } \Delta t \ge \Delta t_{consolidation} \text{ then trigger}
$$

其中：
- $\Delta t$：距离上次巩固的时间间隔
- $\Delta t_{consolidation}$：巩固间隔（不展示真实数值）

**参数解释**：
- $\Delta t_{consolidation}$：巩固间隔，控制巩固的频率

#### 8.2.2 闲置调度

闲置调度：

$$
\text{if } t_{idle} \ge t_{idle,threshold} \text{ then trigger}
$$

其中：
- $t_{idle}$：闲置时间
- $t_{idle,threshold}$：闲置阈值（不展示真实数值）

**参数解释**：
- $t_{idle,threshold}$：闲置阈值，控制何时在闲置时触发巩固

---

**[返回目录](../INDEX.md)** | **[返回本章](./README.md)** | **[上一节](./07.md)** | **下一节：—**
