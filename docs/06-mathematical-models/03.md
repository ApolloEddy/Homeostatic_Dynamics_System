**[返回目录](../INDEX.md)** | **[返回本章](./README.md)** | **[上一节](./02.md)** | **[下一节](./04.md)**

# 06.3 状态更新方程
### 3.1 内驱力系统

#### 3.1.1 社交饱腹感

社交饱腹感更新方程：

$$
H_{\mathrm{social}}(t+\Delta t) = \mathrm{sat}\bigl(H_{\mathrm{social}}(t) - \lambda_{\mathrm{social}} \Delta t + \gamma_{q}\cdot q_t\bigr)
$$

其中：
- \(\lambda_{\mathrm{social}}\)：社交自然衰减率（\(\ge 0\)）
- \(\gamma_q\)：交互质量增益（\(\ge 0\)）
- \(q_t\)：交互质量指标（有界：\([q_{\min}, q_{\max}]\)）
- \(\mathrm{sat}(\cdot)\)：软饱和函数（连续、可导、有界）

#### 3.1.2 认知能量

认知能量更新方程：

$$
H_{\mathrm{energy}}(t+\Delta t) = \mathrm{sat}\bigl(H_{\mathrm{energy}}(t) + \rho_{\mathrm{energy}} \Delta t - \kappa_{\mathrm{task}}\cdot u_t\bigr)
$$

其中：
- \(\rho_{\mathrm{energy}}\)：能量自然恢复率（\(\ge 0\)）
- \(\kappa_{\mathrm{task}}\)：任务消耗系数（\(\ge 0\)）
- \(u_t\)：任务负载/复杂度指标（有界；构造方式由实现决定）

### 3.2 软饱和函数

#### 3.2.1 Sigmoid 函数

Sigmoid 函数：

$$
\mathrm{sat}(x) = \frac{1}{1 + e^{-k_{\mathrm{sat}}(x - x_{\mathrm{mid}})}}
$$

其中：
- \(k_{\mathrm{sat}}\)：软饱和陡峭度（伪变量）
- \(x_{\mathrm{mid}}\)：软饱和中点（伪变量）

**参数解释**：
- \(x_{\mathrm{mid}}\)：sigmoid 形软饱和的中点位置（不展示真实数值）

#### 3.2.2 特性分析

Sigmoid 函数的特性：

- **单调性**：\(\mathrm{sat}'(x) > 0\)
- **有界性**：\(\mathrm{sat}(x) \in (S_{\min}, S_{\max})\)
- **对称性**：\(\mathrm{sat}(x_{\mathrm{mid}} + \delta) = 1 - \mathrm{sat}(x_{\mathrm{mid}} - \delta)\)
- **导数最大**：\(\mathrm{sat}'(x_{\mathrm{mid}})\) 在中点附近取得最大值（具体表达依赖 \(\mathrm{sat}\) 的选型）

#### 3.2.3 压缩系数选择

压缩系数 \(k_{\mathrm{sat}}\) 的选择：

- \(k_{\mathrm{sat}}\) 越大，进入饱和区越快；越小，饱和更缓慢
- 选择原则：在“足够可区分的中间区间”保持敏感度，同时在边界附近提供充分的限幅保护（不提供建议数值）

---

**[返回目录](../INDEX.md)** | **[返回本章](./README.md)** | **[上一节](./02.md)** | **[下一节](./04.md)**
