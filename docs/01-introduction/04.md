**[返回目录](../INDEX.md)** | **[返回本章](./README.md)** | **[上一节](./03.md)** | **[下一节](./05.md)**

# 01.4 社会行为学基础
### 4.1 社交互动理论

社交互动是指个体之间的信息交流和情感连接。HDS 的社交饱腹感变量模拟了社交需求：

#### 4.1.1 社交需求

社交需求是人类的基本需求之一，缺乏社交会导致孤独感、抑郁等问题。HDS 的 $H_{social}$ 变量模拟了社交需求：

$$
H_{\mathrm{social}}(t+\Delta t) = \mathrm{sat}\bigl(H_{\mathrm{social}}(t) - \lambda_{\mathrm{social}}\Delta t + \gamma_q\cdot q_t\bigr)
$$

其中 \(\lambda_{\mathrm{social}}\) 为社交自然衰减率，\(q_t\) 为交互质量指标，\(\mathrm{sat}(\cdot)\) 为软饱和函数（均为符号化表达）。

**参数解释**：
- \(\gamma_q\)：交互质量增益（不展示真实数值）

#### 4.1.2 互动质量

互动质量影响社交需求的满足程度。HDS 通过奖赏预测误差（RPE）评估互动质量：

$$
RPE = r_{actual} - r_{expected}
$$

其中 $r_{actual}$ 是实际奖赏，$r_{expected}$ 是预期奖赏。

### 4.2 边界设定理论

边界设定是指个体在社交互动中设定和维护个人界限的过程。HDS 的边界力度变量模拟了边界设定行为：

#### 4.2.1 边界类型

边界可以分为以下类型：

- **物理边界**：个人空间、身体接触
- **情感边界**：情感表达、情绪分享
- **认知边界**：观点、信念、价值观
- **行为边界**：可接受和不可接受的行为

#### 4.2.2 HDS 边界机制

HDS 的边界力度 \(b_8\) 受 \(N_{\mathrm{5HT}}\) 调节：

$$
b_8 = \text{lerp}(b_{8,high}, b_{8,low}, N_{\mathrm{5HT}})
$$

其中 $b_{8,high}$ 和 $b_{8,low}$ 是边界力度的上下限。

### 4.3 关系维护机制

关系维护是指个体在长期关系中维持和增强关系的行为。HDS 的记忆巩固机制模拟了关系维护：

#### 4.3.1 关系记忆

关系记忆包括偏好、边界、承诺、禁忌等信息。HDS 的 RelationMemory 存储关系记忆：

```json
{
  "category": "relation",
  "content": "用户不喜欢被问及个人隐私",
  "importance": "<IMPORTANCE>",
  "timestamp": "<ISO8601_TIMESTAMP>"
}
```

**参数解释**：
- `<IMPORTANCE>`：重要性/显著性占位符（有界；不展示真实数值）
- `<ISO8601_TIMESTAMP>`：时间戳占位符（示意格式）

#### 4.3.2 关系演化

关系随时间演化，HDS 的离线巩固机制更新关系记忆：

- 高影响事件：提炼为核心记忆
- 低影响闲聊：降采样或丢弃
- 慢变量更新：带限幅 + 可复原

---

**[返回目录](../INDEX.md)** | **[返回本章](./README.md)** | **[上一节](./03.md)** | **[下一节](./05.md)**
